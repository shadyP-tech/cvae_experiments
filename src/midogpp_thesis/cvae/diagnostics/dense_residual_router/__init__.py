"""Exploratory consumed-validation dense residual router diagnostic.

The package initializer intentionally stays import-light.  Import concrete
contracts, configuration, execution, or runner modules at the call site so a
configuration check does not load the full experiment runtime.
"""

__all__: tuple[str, ...] = ()
