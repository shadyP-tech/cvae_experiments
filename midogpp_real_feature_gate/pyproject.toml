[project]
name = "midogpp-real-feature-gate"
version = "0.0.0"
description = "Contract-first MIDOG++ real-feature transfer gate scaffold."
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "scikit-learn",
  "PyYAML",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
