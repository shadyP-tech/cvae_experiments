# Tests

Tests should encode protocol contracts before model-quality assertions.

Initial test priorities:

- config templates declare forbidden router inputs
- support/evaluation split manifests are disjoint
- target expert is excluded from candidate pools
- every candidate expert receives an all-expert downstream score
- downstream oracle is diagnostic only

