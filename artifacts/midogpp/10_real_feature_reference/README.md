# Stage 10 Artifacts

New real-feature reference and source-inner classifier runs are written below
this directory. These outputs support only real-feature transfer claims.

The tuned `xyxy` classifier reference is present locally and on the workstation
at its canonical path with catalog-pinned authoritative hashes and a `PASS`
leakage/protocol report.
