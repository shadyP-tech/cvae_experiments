# Stage 20 Artifacts

New CVAE preservation runs are written below this directory. Preservation
artifacts must not be interpreted as routing or downstream-utility evidence.

The tuned-classifier preservation bundle is present locally and on the
workstation at its canonical path. It is `THESIS-FACING` only for
`cvae_preservation_only` and cannot feed deployable selection.
