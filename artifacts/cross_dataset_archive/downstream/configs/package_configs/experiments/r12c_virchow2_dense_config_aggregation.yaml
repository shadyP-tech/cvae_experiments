# Superseded compatibility marker.
#
# R1.2c-V was extracted into SAIL:
# Source-only Aggregation via Inner-domain Leaveout.
# This path is kept only to prevent stale automation from silently running the
# archived implementation.

experiment:
  name: r12c_virchow2_dense_config_aggregation
  status: superseded_by_sail
  replacement_method: SAIL
  replacement_method_full_name: Source-only Aggregation via Inner-domain Leaveout
  replacement_config: sail/configs/sail_virchow2.yaml
  replacement_validate_command: >-
    PYTHONPATH=sail/src python -m sail.cli validate-config --config sail/configs/sail_virchow2.yaml
  replacement_command: >-
    PYTHONPATH=sail/src python -m sail.cli run --config sail/configs/sail_virchow2.yaml

claim_boundary:
  allowed: >
    Historical R1.2c-V files are provenance for the SAIL extraction.
  forbidden: >
    Do not run this marker as an active experiment config. Use SAIL instead.
