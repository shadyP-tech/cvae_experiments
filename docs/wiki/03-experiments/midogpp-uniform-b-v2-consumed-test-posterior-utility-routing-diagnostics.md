# MIDOG++ Uniform-B V2 Consumed-Test Posterior-Utility Routing Diagnostics

## Scope

This page records the current terminal Stage-90 posterior-utility routing
lineages. They reuse already-consumed MIDOG++ test bytes under separate
six-input fences and ledger amendments. They are bottleneck diagnostics only:
no result may feed a numbered stage, another experiment, policy selection,
promotion, deployment, significance, or a fresh thesis-confirmatory claim.

## PSSCUR V1

Canonical workstation artifact:

```text
artifacts/midogpp/90_oracles_and_diagnostics/
uniform_b_v2_consumed_test_fixed_bank_p_anchored_simultaneous_shift_calibrated_utility_router/v1/
```

Run state is `COMPLETE`; closed-world and two-fresh-process validation report
`PASS`. The primary `PSSCUR_ROBUST_ENVELOPE` authorizes zero routes, produces
zero threshold switches, and exactly reproduces protected portfolio `P` at
equal-center BACC `0.8073167788`. Its information gate is `FAIL`, and the
artifact diagnoses
`POSTERIOR_UTILITY_DOES_NOT_RANK_REALIZED_ACTIONS`.

Publication is `POST_HOC_CONSUMED_TEST_SENSITIVITY`; terminal decision is
`TERMINAL_DIAGNOSTIC_ONLY_DO_NOT_PROMOTE`.

## PCSI-RACR V2

Canonical workstation artifact:

```text
artifacts/midogpp/90_oracles_and_diagnostics/
uniform_b_v2_consumed_test_fixed_bank_p_anchored_route_scoped_boundary_projected_pcsi_policy_regret_router/v2/
```

Run state is `COMPLETE`; validation reports `PASS`, including two independent
fresh-process full scientific reconstructions. The sealed workload contains:

- 810 physical probability cells;
- 218 outer whole-case routes;
- 3,488 outer endpoint fits;
- 436 target posterior fits;
- 1,314 route-scoped utility fits;
- 3,488 policy replays;
- 872 final case predictions including protected `P`.

The primary `PCSI_RACR_PROJECTED_OBSERVED_MAX` envelope authorizes zero target
policies and remains at protected `P` BACC `0.8073167788`. The raw
observed-maximum control also authorizes zero. The projected no-envelope
sensitivity authorizes 11 target policies. The contrast localizes the choke
point to route-scoped uncertainty transport; it does not authorize removal of
the envelope or establish that the 11 sensitivity routes generalize.

Publication is `POST_HOC_CONSUMED_TEST_SENSITIVITY`; terminal decision is
`TERMINAL_DIAGNOSTIC_ONLY_DO_NOT_PROMOTE`; `fresh_evidence=false` and
`may_feed_another_experiment=false`. The canonical artifact records a dirty
repository revision (`d0754475...`), so it is not the primary archival thesis
artifact even though the bundle validates.

## CBPUPR V1 and V2 failed validation; V3 is authorized but not run

Canonical workstation root:

```text
artifacts/midogpp/90_oracles_and_diagnostics/
uniform_b_v2_consumed_test_fixed_bank_p_anchored_route_scoped_center_balanced_posterior_utility_prefix_router/v1/
```

V1 passed workstation admission and completed its expensive physical and
posterior workload. Its runtime report records 81 source streams, 810 physical
classifier cells, 3,488 outer endpoint fits, and 436 target posterior fits;
the scoped leakage report reports `PASS_SCOPED_OWN_ROUTE_NONINTERFERENCE`.

The final run state is nevertheless `FAILED` in phase
`CONTENT_AND_TWO_FRESH_PROCESS_VALIDATION`:

```text
CBPUPR persisted posterior prediction/model lineage drifted.
```

This failure is terminal and nonrecoverable. The presence of diagnostic,
publication, leakage, or runtime files does not make the bundle valid. Do not
quote its provisional routing counts, BACC deltas, randomization result, or
publication payload as scientific evidence. Do not resume, repair, or
quarantine-and-reuse this root in place; any future attempt requires a new
experiment/output identity and a clean, separately validated run.

The separately authorized v2 canonical-row-order repair used a fresh output,
scratch root, four fenced aliases, and amendment. It completed all 81
prediction tasks and 810 physical classifier cells but failed earlier, before
candidate sealing, in phase
`ROUTE_ENDPOINTS_436_POSTERIORS_AND_CANDIDATE_SEAL`:

```text
CBPUPR endpoint worker plan lineage drifted.
```

The production outer plan stored the global nine-center
`PhysicalProbabilitySurface` hash. The endpoint worker validator compared that
plan lineage with a per-center `CenterProbabilitySurface` hash. This hash-scope
mismatch is deterministic and distinct from the v1 row-order defect. It also
explains why narrower worker fixtures that supplied the same per-center hash to
both objects did not expose the production-path failure.

V2 stopped before the terminal-access intent or opened receipt existed. Its
terminal-access journal count is zero, and no terminal-evaluation labels,
terminal metrics, or diagnostic result opened. That clean early stop does not
make v2 recoverable: the launched one-shot identity and amendment are exhausted,
and its partial output or scratch cannot be resumed, repaired in place, rerun
under v2, or consumed by a successor.

V3 is registered as a separately authorized one-shot mechanical successor. Its
repair contract carries an explicit global physical-surface hash through the
outer-endpoint job and spawn payload while retaining the independent per-center
surface validation. Its canonical config, four direct-original v3 aliases,
single-consumer amendment, source/config identities, output/scratch identity,
and v3-only runner are frozen together. It has not been launched, so no v3
output bundle or result exists. Any partial state blocks recovery and rerun;
v1/v2 outputs, scratch, arrays, surfaces, capability history, amendments, and
authorizations remain forbidden inputs.

## Collective interpretation

PSSCUR and PCSI-RACR show that software-valid, leakage-scoped posterior-utility
pipelines can still collapse to protected dense fallback because the available
proxy and transported uncertainty do not reliably rank realized action
utility. PCSI-RACR's no-envelope activity shows sensitivity to conservatism,
not a routing win. CBPUPR adds no scientific result: v1 failed final posterior
lineage validation after terminal access, and v2 failed preterminally on a
global-versus-center surface-hash contract mismatch. V3 is authorized but has
not run, so it is still not a result.

The smallest compliant next scientific experiment is not another post-hoc
router on the same labels. It is one frozen routing hypothesis evaluated once
on a genuinely unconsumed whole-case/patient/slide-disjoint or external/new-
center surface. Until such data exist, these Stage-90 lineages remain terminal
bottleneck analysis only. Any future v3 run over the full already-consumed test
split would still be `POST_HOC_CONSUMED_TEST_SENSITIVITY`,
`TERMINAL_DIAGNOSTIC_ONLY_DO_NOT_PROMOTE`, and `fresh_evidence=false`; it could
not become routing, promotion, deployment, or thesis evidence.
